{
  "$schema": "http://json-schema.org/schema",
  "$id": "teams-app",
  "title": "Teams application schematics",
  "description": "Initializes a new Teams application",
  "additionalProperties": false,
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the project",
      "x-prompt": " What name would you like to use for your project?",
      "$default": {
        "$source": "argv",
        "index": 0
      }
    } 
  }
}